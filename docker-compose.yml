# Oura MCP server — single-file deployment.
# Everything is embedded: no Dockerfile, no separate Python file, and the
# dependency lockfile is inlined below (`oura_requirements`) so every
# container start installs the exact same audited package versions.
# Requires Docker Compose v2.23+ (for inline `configs.content`).
#
# 1. cp .env.example .env and fill in the two tokens.
# 2. docker compose up -d
# (pip installs at container start, ~30s; subsequent restarts re-install
#  unless you keep the pip cache volume below.)

services:
  oura-mcp:
    image: python:3.12-slim
    container_name: oura-mcp
    restart: unless-stopped
    working_dir: /app
    command: >
      sh -c "pip install --no-cache-dir --root-user-action=ignore
      -r /app/requirements.txt && exec uvicorn
      oura_server:app --host 0.0.0.0 --port 8000"
    environment:
      # Values come from .env (see .env.example). `:?` fails fast if unset.
      OURA_ACCESS_TOKEN: ${OURA_ACCESS_TOKEN:?Set OURA_ACCESS_TOKEN in .env (see .env.example)}
      MCP_AUTH_TOKEN: ${MCP_AUTH_TOKEN:?Set MCP_AUTH_TOKEN in .env (see .env.example)}
      OURA_TIMEOUT: ${OURA_TIMEOUT:-30}
    configs:
      - source: oura_server_py
        target: /app/oura_server.py
      - source: oura_requirements
        target: /app/requirements.txt
    ports:
      - "${OURA_MCP_PORT:-8000}:8000"
    volumes:
      - pip-cache:/root/.cache/pip
    mem_limit: 256m
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import httpx,sys; sys.exit(0 if
          httpx.get('http://127.0.0.1:8000/health',
          timeout=5).json().get('status')=='ok' else 1)
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 120s
volumes:
  pip-cache: null
configs:
  oura_server_py:
    content: >
      """

      Oura Ring MCP server — FastMCP.


      Exposes daily activity, readiness, sleep, workouts, heart rate, stress,

      SpO2, sessions, and tags from the Oura v2 API.

      Read-only by design: the Oura API is read-only, so there is nothing to gate.


      Env:
          OURA_ACCESS_TOKEN   Personal access token from cloud.ouraring.com
          MCP_AUTH_TOKEN      Token for /mcp/<token> path auth (unset = no auth)
          OURA_TIMEOUT        Request timeout seconds (default 30)

      Run:
          uvicorn oura_server:app --host 0.0.0.0 --port 8000
      """


      import hmac

      import os

      import time

      from datetime import date, timedelta

      from typing import Optional


      import httpx

      from fastmcp import FastMCP

      from starlette.middleware.base import BaseHTTPMiddleware

      from starlette.responses import JSONResponse


      OURA_BASE = "https://api.ouraring.com/v2/usercollection"

      OURA_TOKEN = os.environ.get("OURA_ACCESS_TOKEN", "")

      AUTH_TOKEN = os.environ.get("MCP_AUTH_TOKEN")

      TIMEOUT = int(os.environ.get("OURA_TIMEOUT", "30"))


      mcp = FastMCP("oura")



      # ---------------------------------------------------------------------------

      # Oura API client

      # ---------------------------------------------------------------------------



      def _get(endpoint: str, params: Optional[dict] = None) -> list:
          """GET an Oura collection endpoint, following next_token pagination."""
          headers = {"Authorization": f"Bearer {OURA_TOKEN}"}
          results: list = []
          params = dict(params or {})
          while True:
              resp = httpx.get(
                  f"{OURA_BASE}/{endpoint}",
                  headers=headers,
                  params=params,
                  timeout=TIMEOUT,
              )
              resp.raise_for_status()
              body = resp.json()
              results.extend(body.get("data", []))
              next_token = body.get("next_token")
              if not next_token:
                  return results
              params["next_token"] = next_token


      def _default_range(date_from: Optional[str], date_to: Optional[str]) -> tuple:
          """Default to the last 7 days if no range given."""
          if not date_to:
              date_to = date.today().isoformat()
          if not date_from:
              date_from = (date.fromisoformat(date_to) - timedelta(days=7)).isoformat()
          return date_from, date_to


      # ---------------------------------------------------------------------------

      # Health check

      # ---------------------------------------------------------------------------



      _HEALTH_TTL_S = 60


      _health_cache: dict = {"ts": None, "ok": False}


      @mcp.custom_route("/health", methods=["GET"])

      async def health(request):
          # Cached so unauthenticated hits can't be used to burn Oura API quota:
          # at most one upstream call per _HEALTH_TTL_S regardless of traffic.
          now = time.monotonic()
          if _health_cache["ts"] is None or now - _health_cache["ts"] >= _HEALTH_TTL_S:
              try:
                  resp = httpx.get(
                      f"{OURA_BASE}/personal_info",
                      headers={"Authorization": f"Bearer {OURA_TOKEN}"},
                      timeout=5,
                  )
                  ok = resp.status_code == 200
              except httpx.HTTPError:
                  ok = False
              _health_cache["ts"] = now
              _health_cache["ok"] = ok
          ok = _health_cache["ok"]
          return JSONResponse({"status": "ok" if ok else "degraded", "oura_api": ok})


      # ---------------------------------------------------------------------------

      # Tools

      # ---------------------------------------------------------------------------



      @mcp.tool()

      def get_daily_activity(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get daily activity summaries: steps, active/total calories, MET minutes
          by intensity, sedentary and resting time, inactivity alerts, and Oura's
          activity score. Dates are LOCAL calendar days as Oura assigns them.

          Useful for correlating movement with insulin sensitivity: low-step,
          high-sedentary days typically show reduced insulin sensitivity.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          days = _get("daily_activity", {"start_date": date_from, "end_date": date_to})
          # Strip the 5-minute MET timeseries and minute-class string: they are
          # enormous and rarely needed for day-level analysis.
          return [
              {
                  "day": d.get("day"),
                  "score": d.get("score"),
                  "steps": d.get("steps"),
                  "active_calories": d.get("active_calories"),
                  "total_calories": d.get("total_calories"),
                  "equivalent_walking_distance_m": d.get("equivalent_walking_distance"),
                  "high_activity_met_minutes": d.get("high_activity_met_minutes"),
                  "medium_activity_met_minutes": d.get("medium_activity_met_minutes"),
                  "low_activity_met_minutes": d.get("low_activity_met_minutes"),
                  "sedentary_met_minutes": d.get("sedentary_met_minutes"),
                  "sedentary_time_s": d.get("sedentary_time"),
                  "resting_time_s": d.get("resting_time"),
                  "non_wear_time_s": d.get("non_wear_time"),
                  "inactivity_alerts": d.get("inactivity_alerts"),
                  "average_met_minutes": d.get("average_met_minutes"),
              }
              for d in days
          ]


      @mcp.tool()

      def get_daily_readiness(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get daily readiness: overall score, temperature deviation from baseline,
          and contributor scores (HRV balance, resting heart rate, recovery index,
          sleep balance, activity balance).

          Temperature deviation is a useful early-illness signal, and illness is a
          major driver of temporary insulin resistance.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          days = _get("daily_readiness", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": d.get("day"),
                  "score": d.get("score"),
                  "temperature_deviation_c": d.get("temperature_deviation"),
                  "temperature_trend_deviation_c": d.get("temperature_trend_deviation"),
                  "contributors": d.get("contributors"),
              }
              for d in days
          ]


      @mcp.tool()

      def get_daily_sleep(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get daily sleep scores and contributors (deep, REM, efficiency, latency,
          restfulness, timing, total sleep).

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          days = _get("daily_sleep", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": d.get("day"),
                  "score": d.get("score"),
                  "contributors": d.get("contributors"),
              }
              for d in days
          ]


      @mcp.tool()

      def get_sleep_periods(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get detailed sleep periods: bedtimes, durations by stage, efficiency,
          average heart rate, average HRV, and lowest heart rate.

          Overnight HRV and resting HR trends often track systemic stress and
          illness, both of which affect insulin needs.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          periods = _get("sleep", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": p.get("day"),
                  "type": p.get("type"),
                  "bedtime_start": p.get("bedtime_start"),
                  "bedtime_end": p.get("bedtime_end"),
                  "total_sleep_duration_s": p.get("total_sleep_duration"),
                  "time_in_bed_s": p.get("time_in_bed"),
                  "efficiency": p.get("efficiency"),
                  "deep_sleep_duration_s": p.get("deep_sleep_duration"),
                  "rem_sleep_duration_s": p.get("rem_sleep_duration"),
                  "light_sleep_duration_s": p.get("light_sleep_duration"),
                  "awake_time_s": p.get("awake_time"),
                  "average_heart_rate": p.get("average_heart_rate"),
                  "lowest_heart_rate": p.get("lowest_heart_rate"),
                  "average_hrv": p.get("average_hrv"),
              }
              for p in periods
          ]


      @mcp.tool()

      def get_workouts(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get logged workouts: activity type, intensity, calories, and start/end
          times. Workout timing matters for insulin sensitivity windows (increased
          sensitivity typically persists for hours after activity).

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          workouts = _get("workout", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": w.get("day"),
                  "activity": w.get("activity"),
                  "intensity": w.get("intensity"),
                  "calories": w.get("calories"),
                  "start_datetime": w.get("start_datetime"),
                  "end_datetime": w.get("end_datetime"),
                  "source": w.get("source"),
              }
              for w in workouts
          ]


      @mcp.tool()

      def get_activity_summary(days: int = 14) -> dict:
          """Compact multi-day activity summary for correlation with glucose data:
          per-day steps, sedentary hours, MET minutes, and activity score, plus
          period averages. Prefer this over get_daily_activity for longer ranges.

          Args:
              days: Number of days to summarize, ending today (default 14)
          """
          date_to = date.today().isoformat()
          date_from = (date.today() - timedelta(days=days)).isoformat()
          daily = _get("daily_activity", {"start_date": date_from, "end_date": date_to})
          if not daily:
              return {"error": "No activity data found", "from": date_from, "to": date_to}

          rows = {
              d["day"]: {
                  "steps": d.get("steps"),
                  "score": d.get("score"),
                  "sedentary_hours": round((d.get("sedentary_time") or 0) / 3600, 1),
                  "active_met_minutes": round(
                      (d.get("high_activity_met_minutes") or 0)
                      + (d.get("medium_activity_met_minutes") or 0)
                      + (d.get("low_activity_met_minutes") or 0),
                      0,
                  ),
              }
              for d in daily
          }
          steps = [r["steps"] for r in rows.values() if r["steps"] is not None]
          return {
              "from": date_from,
              "to": date_to,
              "days": rows,
              "averages": {
                  "steps": round(sum(steps) / len(steps)) if steps else None,
                  "sedentary_hours": round(
                      sum(r["sedentary_hours"] for r in rows.values()) / len(rows), 1
                  ),
              },
          }


      @mcp.tool()

      def get_heart_rate(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get per-day heart rate summaries built from the intraday HR
          timeseries: min/avg/max bpm, reading count, and average bpm by source
          (awake, rest, sleep, session, workout). The raw timeseries is
          summarized per day because it is far too large to return directly.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          points = _get(
              "heartrate",
              {
                  "start_datetime": f"{date_from}T00:00:00",
                  "end_datetime": f"{date_to}T23:59:59",
              },
          )
          days: dict = {}
          for p in points:
              bpm = p.get("bpm")
              ts = p.get("timestamp") or ""
              if bpm is None or len(ts) < 10:
                  continue
              d = days.setdefault(ts[:10], {"bpms": [], "sources": {}})
              d["bpms"].append(bpm)
              d["sources"].setdefault(p.get("source") or "unknown", []).append(bpm)
          return [
              {
                  "day": day,
                  "readings": len(d["bpms"]),
                  "bpm_min": min(d["bpms"]),
                  "bpm_avg": round(sum(d["bpms"]) / len(d["bpms"]), 1),
                  "bpm_max": max(d["bpms"]),
                  "avg_by_source": {
                      s: round(sum(v) / len(v), 1)
                      for s, v in sorted(d["sources"].items())
                  },
              }
              for day, d in sorted(days.items())
          ]


      @mcp.tool()

      def get_daily_stress(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get daily stress summaries: seconds spent in high-stress and
          high-recovery zones plus Oura's day classification (restored /
          normal / stressful). Sustained stress raises cortisol, which tends
          to increase insulin needs.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          days = _get("daily_stress", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": d.get("day"),
                  "stress_high_s": d.get("stress_high"),
                  "recovery_high_s": d.get("recovery_high"),
                  "day_summary": d.get("day_summary"),
              }
              for d in days
          ]


      @mcp.tool()

      def get_daily_spo2(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get nightly blood oxygen: average SpO2 percentage and breathing
          disturbance index (higher = more disturbed breathing during sleep).

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          days = _get("daily_spo2", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": d.get("day"),
                  "spo2_avg_percent": (d.get("spo2_percentage") or {}).get("average"),
                  "breathing_disturbance_index": d.get("breathing_disturbance_index"),
              }
              for d in days
          ]


      @mcp.tool()

      def get_sessions(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get logged sessions (meditation, breathing, nap, relaxation,
          rest): type, mood, and start/end times. Heart rate and HRV
          timeseries are omitted to keep responses small.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          sessions = _get("session", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "day": s.get("day"),
                  "type": s.get("type"),
                  "mood": s.get("mood"),
                  "start_datetime": s.get("start_datetime"),
                  "end_datetime": s.get("end_datetime"),
              }
              for s in sessions
          ]


      @mcp.tool()

      def get_tags(
          date_from: Optional[str] = None,
          date_to: Optional[str] = None,
      ) -> list[dict]:
          """Get user-entered tags and notes (enhanced tags): tag type, custom
          name, free-text comment, and when they apply. Tags mark events like
          illness, travel, or alcohol, so they are useful for explaining
          anomalies in other metrics.

          Args:
              date_from: Start date YYYY-MM-DD (default: 7 days before date_to)
              date_to: End date YYYY-MM-DD inclusive (default: today)
          """
          date_from, date_to = _default_range(date_from, date_to)
          tags = _get("enhanced_tag", {"start_date": date_from, "end_date": date_to})
          return [
              {
                  "start_day": t.get("start_day"),
                  "end_day": t.get("end_day"),
                  "tag_type_code": t.get("tag_type_code"),
                  "custom_name": t.get("custom_name"),
                  "comment": t.get("comment"),
                  "start_time": t.get("start_time"),
                  "end_time": t.get("end_time"),
              }
              for t in tags
          ]


      # ---------------------------------------------------------------------------

      # Auth middleware

      # ---------------------------------------------------------------------------



      def _token_ok(candidate: str) -> bool:
          """Constant-time comparison to avoid timing side channels."""
          return hmac.compare_digest(candidate.encode(), AUTH_TOKEN.encode())


      class TokenPathAuthMiddleware(BaseHTTPMiddleware):
          """Auth via token in URL path: /mcp/<token> or Bearer header.

          Accepts:
            - POST /mcp/<token>  (for Claude.ai connectors)
            - POST /mcp with Authorization: Bearer <token>  (for Claude Code CLI)
            - GET /health  (always open, no auth)
          """

          async def dispatch(self, request, call_next):
              path = request.url.path

              if path == "/health":
                  return await call_next(request)

              if not AUTH_TOKEN:
                  return await call_next(request)

              if path.startswith("/mcp/"):
                  token = path[len("/mcp/"):]
                  if _token_ok(token):
                      request.scope["path"] = "/mcp"
                      return await call_next(request)
                  return JSONResponse({"error": "Unauthorized"}, status_code=401)

              if path == "/mcp":
                  auth_header = request.headers.get("authorization", "")
                  if auth_header.startswith("Bearer ") and _token_ok(auth_header[7:]):
                      return await call_next(request)
                  return JSONResponse({"error": "Unauthorized"}, status_code=401)

              return JSONResponse({"error": "Not Found"}, status_code=404)


      app = mcp.http_app(stateless_http=True)

      app.add_middleware(TokenPathAuthMiddleware)
  # Full pip freeze from a verified-working install (pip-audit clean of
  # reachable vulns as of 2026-08-12). Regenerate after upgrades with:
  #   docker exec oura-mcp pip freeze
  oura_requirements:
    content: |
      annotated-doc==0.0.5
      annotated-types==0.8.0
      anyio==4.14.2
      attrs==26.1.0
      Authlib==1.7.2
      beartype==0.22.9
      burner-redis==0.1.7
      cachetools==7.1.7
      certifi==2026.7.22
      cffi==2.1.1
      click==8.4.2
      cloudpickle==3.1.2
      cronsim==2.7
      cryptography==50.0.0
      cyclopts==4.22.5
      diskcache==5.6.3
      dnspython==2.8.0
      docstring_parser==0.18.0
      email-validator==2.3.0
      exceptiongroup==1.3.1
      fakeredis==2.34.1
      fastmcp==2.14.7
      h11==0.16.0
      httpcore==1.0.9
      httpx==0.28.1
      httpx-sse==0.4.3
      idna==3.18
      jaraco.classes==3.4.0
      jaraco.context==6.1.2
      jaraco.functools==4.6.0
      jeepney==0.9.0
      joserfc==1.7.4
      jsonref==1.1.0
      jsonschema==4.26.0
      jsonschema-path==0.5.0
      jsonschema-specifications==2025.9.1
      keyring==25.7.0
      lupa==2.8
      markdown-it-py==4.2.0
      mcp==1.29.0
      mdurl==0.1.2
      more-itertools==11.1.0
      openapi-pydantic==0.5.1
      opentelemetry-api==1.44.0
      packaging==26.3
      pathable==0.6.0
      pathvalidate==3.3.1
      platformdirs==4.11.2
      prometheus_client==0.26.0
      py-key-value-aio==0.3.0
      py-key-value-shared==0.3.0
      pycparser==3.0
      pydantic==2.13.4
      pydantic-settings==2.15.0
      pydantic_core==2.46.4
      pydocket==0.24.1
      Pygments==2.20.0
      PyJWT==2.13.0
      pyperclip==1.11.0
      python-dotenv==1.2.2
      python-json-logger==4.1.0
      python-multipart==0.0.32
      PyYAML==6.0.3
      redis==8.1.0
      referencing==0.37.0
      rich==15.0.0
      rich-rst==2.1.0
      rpds-py==2026.6.3
      SecretStorage==3.5.0
      shellingham==1.5.4
      sortedcontainers==2.4.0
      sse-starlette==3.4.8
      starlette==1.6.0
      typer==0.27.1
      typing-inspection==0.4.4
      typing_extensions==4.16.0
      uncalled-for==0.4.0
      uvicorn==0.52.1
      websockets==17.0.1
networks: {}
